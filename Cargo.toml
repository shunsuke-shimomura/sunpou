[package]
name = "sunpou"
version = "0.1.0"
edition = "2024"
description = "Compile-time dimensional analysis for linear algebra — scalars, vectors, matrices with SI units, coordinate frames, and prefix scaling"
license = "MIT OR Apache-2.0"

[dependencies]
typenum = { version = "1", default-features = false }
nalgebra = { version = "0.33", default-features = false, features = ["libm"] }

[dev-dependencies]
uom = { version = "0.36", default-features = false, features = ["f64", "si"] }
criterion = { version = "0.5", features = ["html_reports"] }
trybuild = "1"

[[bench]]
name = "zero_cost"
harness = false
